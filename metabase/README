Readme

If you have connection issue in metabase Web-UI:

- Error: RSA public key is not available client side

- Use a secure connection SSL:
- Enable SSL
- Additional JDBC connection string options:
- trustServerCertificate=true

connections on Unix to localhost are made using a Unix socket file by default:
- To force TCP/IP transport to be used instead, specify a --protocol option
- mysql --host=localhost --protocol=TCP